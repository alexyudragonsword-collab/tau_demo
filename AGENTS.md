# tau_demo 协作规则

> 本项目使用 Project Cairn 组织项目知识：`AGENTS.md` 是规则与导航入口，`cairn/` 是项目知识/状态层。
> 同目录的 `CLAUDE.md` 只应包含一行 `@AGENTS.md`，使 Claude Code 读到同一套规则；Codex 直接读本文件。

## 项目一句话

对何庭波《A Time Scaling Theory for Multi-Layer Electronic Systems》（ChinaXiv:202605.00224）核心主张的**独立仿真验证**：四层级联模型 + 对抗性审阅。**数字本身就是产品**——报告里的每个结论都由代码算出。

> 本文件由 `cairn init` 生成，已填入本项目自身的定位与 provider 配置；其他项目应各自运行 init 后再复用。

## Init 配置

- Graduation provider(s): 暂无（暂缓对接 —— 首次毕业时再连接知识库）
- 知识库索引: 尚未配置
- 毕业目标: 尚未配置

## 进入项目后的阅读顺序

1. 先读本文件（AGENTS.md）。
2. 读根目录 `ROADMAP.md`（本项目的路线图与开放问题；Cairn 未另建 `cairn/ROADMAP.md`，以免与之重复）。
3. 读 `cairn/LOG.md` 最新条目（倒序，最新在最上）了解近期进展与关键决策。
4. 按任务需要读相关 `cairn/` 知识专题文档。

## 项目铁律（违反即损害交付物的论证力）

1. **论文数据永远不进入模型。** `tau_sim/params.py` 的参数全部来自独立公开文献；论文自称数字只存在于 `params.PAPER_TARGETS`，仅用于画对照柱。**绝不可为了"让结果更接近论文"而调参数。**
2. **`tau_lab_model.js` 是手工移植，不会自动跟随 Python。** 改了 `tau_sim/` 任何公式或常量后必须跑 `python tests/test_crosscheck.py`。
3. **黄金值测试失败时不要直接改期望值。** 先判断是 bug 还是有意的模型改动；若是后者，要同步更新测试期望值 → REPORT.md → CRITIQUE.md → 重新出图与页面。
4. **改模型公式后必跑三件事**：`test_regression.py`、`test_crosscheck.py`、`sensitivity.py`。

> 常用命令、架构速览、已知陷阱见 `cairn/项目约定与陷阱.md`；详细架构见 `ARCHITECTURE.md`。

## 文档职责

| 文件 | 角色 | 维护方式 |
|---|---|---|
| `AGENTS.md`（根目录） | 规则与导航 | 很少变动，≤ 60 行 |
| `CLAUDE.md`（根目录） | 一行 `@AGENTS.md` 桩文件 | 写一次，不再改动 |
| `ROADMAP.md`（根目录） | 路线图与待办 | 就地更新，保持精简 |
| `cairn/LOG.md` | 时序日志 | 新条目加在最上（最新在前），每条 ≤ 20 行，只写摘要与指针 |
| `cairn/<topic>.md` | 知识专题文档（当前真相） | 就地更新；陷阱写进正文小节并用 `contains` 标记；修订留 LOG 指针 |
| `cairn/Reference/` | 外部原始输入 | 按需创建；只追加 |
| `cairn/Cited.md` | 知识库引用清单 | 只放指针，不复制原文 |

> 其余文件只在出现具体信号时创建（需要记录一个决策、解决了一个陷阱、目标跨越单次会话），不预建空壳。工程资产（被代码或流程消费的契约/配置/规格）不归本系统管理，留在代码树里，不进 `cairn/`。

## 冲突仲裁规则

- 优先级：**知识专题文档 > LOG 历史**；规则层面的冲突由本文件裁定。
- 模型/结论层面以 `cairn/` 专题文档与 `REPORT.md` 的最新记录为准，而非更早的 LOG 条目。

## 知识库消费反射

- 在开展"其可复用内核够格毕业"的工作前，先查阅本项目自己的 `cairn/` 知识专题文档；目前尚未连接外部知识库（provider 暂缓对接 —— 见上方 Init 配置），外部索引检查与 `cairn/Cited.md` 引用将在连接后启用。

## 文档协作规则

- 动手改之前先判断用户要的是"讨论/建议"还是"直接改文档"；当用户说"先看看/先评估"时先给分析，不要直接重写正式文档。
- 修正过往判断时追加更正说明，不要静默覆盖。
- 不要把未经确认的判断写成既定事实。

## 知识沉淀规则

- 每次实质性推进后，在 `cairn/LOG.md` 顶部加一条（摘要 + 指针）；结论沉淀进 `cairn/` 知识专题文档。
- **完成回复闸门：** 在任何完成性表述之前——包括但不限于工作已完成/已实现、已定稿、已更新、已同步、已验证或测试通过；问题已修复或已解决；交付物可用；工作已结束等语义等价表述——先执行 `references/maintenance.md` 中的 Cairn 检查点，只更新其触发矩阵要求的记录并核验后再回复。明确的只读/不修改要求禁止 Cairn 写入。
- 跨项目可复用的经验，在连接知识库后通过毕业机制沉淀（provider 暂缓对接 —— 见上方 Init 配置）。
