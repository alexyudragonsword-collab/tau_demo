# 待办与未来工作

本文件把散落在 `REPORT.md §7`、`CRITIQUE.md`、`ARCHITECTURE.md §7` 里的欠账
收敛成一张有优先级、可认领的清单。

> **两类事项性质不同，切勿混淆：**
> - 🔵 **本仿真的欠账**——我们能做但还没做的，做了会让结论更强或更可信。
> - ⚪ **论文的开放问题**——需要论文作者或第三方回答的，不在本仓库的能力范围内，
>   仅记录以便追踪。
>
> 每项标注 **结论影响**：完成后会不会改变 REPORT.md / CRITIQUE.md 中的论断。

---

## P1 — 高优先级（会实质增强论证力）

### 🔵 R1. 补一个能量模型
**结论影响：会强化 CRITIQUE S5，并可能新增结论。**

CRITIQUE S5 批评论文存在"能量盲区"（τ 是时间律不是焦耳律，作者自认未闭合）。
但**本仿真自己同样没有能量模型**——我们只能引用 CloudMatrix 的功耗数据做旁证，
无法自己算。这是目前最大的不对称：批评了对方没做的事，自己也没做。

- 做法：在四层模型上加一层能量记账（pJ/bit 搬运 + 每层动态/静态功率 +
  系统功率预算），使 `cascade` 能同时输出 τ 和 J；
- 收益：S5 从"指出问题"升级为"量化问题"，并能回答"τ-first 路线在电网约束下
  能走多远"这个论文回避的问题；
- 参考：Horowitz ISSCC'14（能耗表）、Boroumand ASPLOS'18（数据搬运占比）。

### 🔵 R2. 建模计算-通信重叠（overlap）
**结论影响：改变绝对数值，不改变结论方向。**

现代训练框架可隐藏 30–60% 的通信时间，本模型未建模，故扩展效率是**保守下界**。

- 做法：在 `layer4_system` / `cascade.iteration_tau` 中引入可配置的 overlap 比例；
- 预期：实验 C 中通信分量的绝对占比下降，但**瓶颈迁移的次序不变**
  （REPORT §7 已如此声明，届时可从"声明"变为"验证"）；
- 注意：这会动黄金值 → 需同步更新 `tests/test_regression.py` 与 REPORT 正文。

### 🔵 R3. 让密度项从"校准"变成"预测"
**结论影响：会把 P2 的一项从一致性检验升级为独立预测。**

目前折叠密度 155→238 MTr/mm² 依赖一个校准过的面积开销系数，这是**全项目唯一
的校准例外**（REPORT §4.2 已标注）。

- 做法：建立真正的面积模型（TSV/键合焊盘占位、冗余、布局效率），使密度成为
  模型输出而非拟合结果；
- 收益：消除 CRITIQUE S3 中"+55% 是面积记账"这一自我保留。

---

## P2 — 中优先级（提升稳健性与可信度）

### 🔵 R4. UB 参数改为不确定性区间
**结论影响：结论表述从点值变为区间，更抗质疑。**

fabric 参数取自 UB-Mesh / CloudMatrix 论文（华为自述，无第三方基准）。

- 做法：把 UB 延迟/带宽建成区间（如延迟 ×1–10），在敏感性面板与报告中输出
  "在任何合理取值下 regime 结构不变"这类区间结论；
- 现状：`tau_lab` 的面板 3 已支持手动压力测试，但报告仍用点值叙述。

### 🔵 R5. 热模型二维化
**结论影响：可能收紧同构折叠的下界。**

现为一维热阻网络，未含横向热扩散与热点叠加（imec 仿真显示最差同构分区
可达 +44% 温升）。当前同构 4 层 f=0.60 应视为**保守下界**。

- 做法：引入横向导热与热点分布；
- 注意："异构折叠近乎无损"这一结论有 AMD X3D 产品数据交叉佐证，预计不受影响。

### 🔵 R6. 扩展 DES 覆盖面
**结论影响：无（验证手段增强）。**

SimPy 目前只验证 ring allreduce 与 all-to-all 两种集合操作。可扩展到
分层 allreduce、流水并行的气泡等，让解析模型的更多部分有事件级背书。

---

## P3 — 工程与交付

| # | 事项 | 说明 |
|---|---|---|
| 🔵 R7 | README 加 CI 徽章 | 让"测试通过"可见 |
| 🔵 R8 | 部署 Streamlit 应用 | 目前只能本地跑；部署后可直接分享链接 |
| 🔵 R9 | **PPTX 视觉校验** | 沙箱里 LibreOffice 无法渲染 PPTX，8 页汇报**至今未做过像素级检查**（仅通过结构校验与内容 QA）。需在真实 PowerPoint 中过一遍 |
| 🔵 R10 | `ARCHITECTURE.md` 英文版 | 目前仅中文，与其余文档的双语标准不一致 |
| 🔵 R11 | 单元测试细化 | 现有测试锁的是端到端黄金值；可为 `davis_distribution`、`rho_cu` 等基础函数加窄单测，失败时定位更快 |

---

## 论文的开放问题（不在本仓库能力范围）

以下三条来自 `CRITIQUE.md` 的结尾，需要论文作者或第三方回答，记录于此以便追踪：

- ⚪ **O1. α 的可测量定义与分层分解**——把"10×/年"拆成硬件/算法/精度各占多少。
  本仿真已给出硬件侧的上界（≈2.0/年，含精度 ≈2.6/年），缺的是作者对口径的界定。
- ⚪ **O2. 为 τ 配强制的能量协同指标**——使 τ-first 不能 license 撞电网的设计。
  （若 R1 完成，我们可以提出一个候选指标。）
- ⚪ **O3. 公开 τ-profile 基准与受控反事实**——让"τ 优于纯几何缩放"可被第三方复现。
  论文自认现有 benchmark 测不了 τ-profile，但结论又建立在未公开的内部测量上。

---

## 已明确不做

- **Windows exe 打包**：讨论后放弃（Streamlit 打包体积 300–500MB，且本环境为
  Linux 无法交叉编译；`tau_lab.html` 零依赖单文件已覆盖"发给别人就能跑"的需求）。
- **把论文数据用作模型输入**：违反项目根本原则，永久不做（见 `CLAUDE.md` 铁律 1）。

---

## 认领方式

本仓库无 issue 模板；直接在本文件中把对应项标记为进行中并注明负责人即可，
完成后移入 `CHANGELOG.md` 的对应版本条目。
